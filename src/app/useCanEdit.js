// import React, { useMemo } from "react";
// import { canEditPSC } from "./pscPermissions";

// /**
//  * useCanEdit: Only recalculates when user or PSC changes.
//  * @param {Object} psc
//  * @param {Object} user
//  */
// export function useCanEdit(psc, user) {
//   return useMemo(() => canEditPSC(psc, user), [psc, user]);
// }